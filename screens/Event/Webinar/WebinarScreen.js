import { View, Text } from 'react-native'
import React from 'react'
import { useNavigation } from "@react-navigation/native";

const WebinarScreen = () => {
    const Navigation = useNavigation();
  return (
    <View>
      <Text>WebinarScreen</Text>
    </View>
  )
}

export default WebinarScreen